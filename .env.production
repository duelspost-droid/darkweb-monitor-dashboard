# 프로덕션 빌드(next build)에 인라인되는 공개 클라이언트 설정.
# 관리자 이메일은 비밀이 아니며(로그인 폼에서 이 계정으로 인증, 게이트는 비밀번호) 클라이언트 번들에 노출된다.
# Supabase URL/anon 키는 CI(GitHub Actions)의 워크플로 env(secrets)로 주입되므로 여기 두지 않는다.
NEXT_PUBLIC_ADMIN_EMAIL=duels@jbfg.com
