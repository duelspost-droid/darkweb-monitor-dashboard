{
  "$comment": "다크웹/유출 모니터링 대상. (1) HIBP_API_KEY 가 있으면 domains 를 도메인 전수 검색. (2) 키가 없으면 무료 XposedOrNot 로 accounts(개별 이메일)를 조회. 실제 이메일은 여기 대신 monitor_config.local.json(.gitignore) 에 넣으면 커밋되지 않는다. 저장은 항상 마스킹만.",
  "domains": ["jbfg.com"],
  "accounts": [],
  "minSeverity": "low"
}
